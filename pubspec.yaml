name: song_pocket
description: Local-first mobile audio recorder and player PWA.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  web: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
